import 'package:flutter/material.dart';

class ShowPostWidget extends StatelessWidget {
  const ShowPostWidget({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
